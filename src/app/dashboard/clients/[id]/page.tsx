// ClientDetailWorkspace now reads the id from useParams() internally.
// No static data import needed.
import { ClientDetailWorkspace } from "../../../../../clients/client-detail/ClientDetailWorkspace";

export default function ClientDetailPage() {
  return <ClientDetailWorkspace />;
}
